{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "buildCommand": "cd backend && chmod +x mvnw && ./mvnw clean package"
    },
    "deploy": {
        "startCommand": "cd backend && java -jar target/*.jar"
    }
}