# Basic Config
server.port=${SERVER_PORT:8080}
spring.application.name=backend

# Database - MongoDB Configuration
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://career_advisor:CareerAdvisorDB1405@cluster0.94w7mbi.mongodb.net/?appName=Cluster0}
spring.data.mongodb.auto-index-creation=true

# Email setup
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:465}
spring.mail.username=${MAIL_USERNAME:ayushmistry1405@gmail.com}
spring.mail.password=${MAIL_PASSWORD:gdcgebslvdwaghsb}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.debug=${MAIL_DEBUG:true}
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000
spring.mail.default-encoding=UTF-8

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG

spring.main.allow-circular-references=true

wit.ai.token=${WIT_AI_TOKEN:UVKINRO2WWTC2ZUWVWPNENQBNWOSPR24}

# CORS
app.cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5500,http://127.0.0.1:3000,http://127.0.0.1:5500}

# JWT
jwt.secret=${JWT_SECRET:change_this_to_a_32+_char_long_random_key________________________________}
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}
