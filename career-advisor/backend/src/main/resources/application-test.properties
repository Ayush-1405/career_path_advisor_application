# Test Configuration - Use MongoDB with a different test database
server.port=8081
spring.application.name=backend-test

# MongoDB Test Configuration
spring.data.mongodb.uri=mongodb+srv://career_advisor:CareerAdvisorDB1405@cluster0.94w7mbi.mongodb.net/career_advisor_test?appName=Cluster0
spring.data.mongodb.auto-index-creation=true

# Security Configuration (Defaults for tests)
jwt.secret=test_secret_key_for_development_only_32_chars
jwt.expiration-ms=86400000

# Other required properties
wit.ai.token=test-token
app.cors.allowed-origins=http://localhost:3000

# Logging
logging.level.org.springframework=INFO
logging.level.com.advisor=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG
